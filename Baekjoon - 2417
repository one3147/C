#include <stdio.h>
#include <math.h>
int main()
{   
    long long int a;
    scanf("%lld", &a);
    long long int b = sqrt(a);
    if (b * b == a)
        printf("%lld", b);

    else if (b * b < a)
        printf("%lld", b + 1);

    else
        printf("%lld", b);

    return 0;
}
