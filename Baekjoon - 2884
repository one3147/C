#include<stdio.h>

int main() {
	int a, b;
	scanf("%d %d", &a, &b);

	if (b - 45 >= 0) {
		printf("%d %d", a, b - 45);
	}
	
	if (b - 45 < 0) {
		int c = b - 45;
		if (a == 0) {
			printf("23 %d", 60 + c);
		}
		else {
			printf("%d %d", a - 1, 60 + c);
		}
	}




	return 0;
}
